<div class="show">
  <div class="green">
  <% if flash[:notice] %>
     <p id="notice">
       <%= flash[:notice] %>
     </p>
   <% end %>
   </div>
<table>
  <tr>
    <th>Title:</th>
    <td><%= @book.title %></td>
  </tr>
  <tr>
    <th>Body:</th>
    <td><%= @book.body %></td>
  </tr>
</table>
<%= link_to "Edit", edit_book_path(@book.id) %>
<%= link_to "Back", books_path(@book.id) %>
</div>
